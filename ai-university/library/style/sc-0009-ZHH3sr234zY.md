---
id: sc-0009
source_video: ZHH3sr234zY
views: "523K"
length: "1:31:43"
---
# Hook (0:00–~0:01)
Curriculum-promise open framed as a "masterclass": explicitly states the arc
(beginner → AI-agent builder by the end). Light self-aware joke about note-taking,
then straight into definitions. No cold result demo — the promise of a complete
ordered journey is the hook. Very low-key, lecture-style energy versus the flashier
creator-course openers.

# Beat map
Classic slides-then-build lecture, six labeled parts.
- Intro: what n8n is; why automate; why learn it (slides).
- Part 1: setup (self-hosted vs cloud) + interface tour, community templates.
- Part 2: core concepts — four node types — then a live build (Sheets → LLM → Gmail).
- Part 3: RAG + vector DBs (slides) → live build (ingest PDF to Pinecone; chat agent).
- Part 4: workflows-as-tools and agent hierarchies (slides + tour of prebuilt agents).
- Part 5: APIs & HTTP requests (slides + live GET/POST/webhook examples).
- Part 6: error workflows, optimization best practices, next steps, community CTA.

# Retention devices
Consistent "slides → then we hop into n8n and build it live" rhythm sets expectations
and rewards patience. Concrete analogies for abstract ideas (recipe/restaurant for
workflow/node/execution; restaurant for API/endpoint/call/HTTP). Deliberately shows
failures and fixes live (a date-format bug; a forced error) to model debugging via
the executions view. Cross-links to his other videos for deeper dives on specific
agents (open loops pointing off-video). Section recaps at part boundaries.

# CTA placement
Minimal mid-roll selling; CTAs cluster at the very end (free community for templates
and collaboration, plus like/subscribe). Mid-video "asks" are actually pedagogical
nudges ("get in and build, let it fail"). Repeated pointers to his other videos
function as internal-CTA retention rather than external asks.
