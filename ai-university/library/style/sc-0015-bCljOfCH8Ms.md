---
id: sc-0015
source_video: bCljOfCH8Ms
views: "395K"
length: "2:33:21"
---
# Hook (0:00–~0:02)
Outcome-promise cold open: states the end result ("by the end you'll know exactly
how to build X, even as a total beginner") before any teaching. Immediately layers
free-artifact bait (a downloadable setup guide) and creator credibility (prior
exit, community size, upload cadence) to justify the runtime. No product on screen
yet — the promise and the free asset are the hook, not a result demo.

# Beat map
Front-loaded conceptual frameworks, then a long hands-on middle, then cadence/scale.
- Concept: what an "operating system" for AI is; tool-agnostic framing.
- Framework 1: the Three M's (mindset/method/machine) read off a doc.
- Framework 2: the Four C's (context/connections/capabilities/cadence).
- Planning: paper exercise mapping seven business data domains.
- Build: clone repo → project tour (.claude, context, references, instruction file).
- Onboarding: 7-question interview scaffolds day-one files.
- Connections: wiring a first integration via API endpoints (+ MCP-vs-API aside).
- Capabilities: long embedded skills mini-course (anatomy, progressive disclosure,
  six-step build, live build, debugging table); embedded Google Workspace CLI clip.
- Cadence: routines vs. scheduled tasks vs. loop (two embedded clips); reminders.
- Knowledge layer: embedded Karpathy-style LLM-wiki clip (Obsidian graph build).
- Wrap: artifact dashboards + proof-of-concept mindset; success criteria; CTA.

# Retention devices
Repeated open loops ("we'll come back to this later"). Multiple embedded standalone
videos spliced in as chapters (skills, GWS CLI, routines, loop, LLM wiki), each with
its own mini-hook — course-of-courses structure. Whiteboard/graph doodles drawn live
(productivity dip, learning curve). Named mnemonics (Three M's, Four C's, WAT) as
memory hooks. On-screen redaction ("I'll blur this") teases real private data as
proof. Frequent reassurance beats to fight overwhelm ("don't stress, this evolves").
Live agent runs shown in real time so viewers watch work happen.

# CTA placement
Free-community CTA seeded early (hook) as the gateway to the required free resources
(repo + docs), so the CTA is structurally load-bearing to follow along, not just an
ask. Repeated at each artifact drop ("I'll leave this in the free community"). Soft
product mention for a voice tool recurs as an affiliate aside. Closes with
like/subscribe and a next-video handoff (context management).
