---
id: sc-0021
source_video: A0OwvNOLNlw
views: "342K"
length: "7:04"
---
# Hook (0:00–~0:20)
Pattern: outcome promise → "quickest and easiest way." States the end state immediately (message a business number, an AI agent replies/books) and frames the video as the fastest path to it, then jumps straight into the tool with zero preamble. Tight, utility-first hook for a short tutorial.

# Beat map (approx.)
- 0:00–0:20 Promise + what we're building.
- 0:20–2:30 Credential #1 (trigger): walk through the developer console — create business app, confirm account, copy app ID/secret into the trigger node; confirm green.
- 2:30–3:15 Bridge: explain there are *two* credential types (the key confusion point) before hitting the wall.
- 3:15–4:45 Credential #2 (send node): API setup, test number + recipient, generate access token, paste token + business account ID; confirm green.
- 4:45–6:00 Wire and test the flow: add agent + model, map incoming body field, execute-to-listen, live test.
- 6:00–6:40 Map the agent output back to the send node; final live joke test end to end.
- 6:40–7:04 Cost caution (public number = per-message spend) → community CTA → like/outro.

# Retention devices
- "Green connection successful" moments serve as repeated micro-payoffs pacing the setup.
- Explicit naming of the tricky part ("here's the issue — this is a different credential") pre-empts the exact confusion that causes drop-off.
- Real live tests (typing into the actual chat, showing the reply) provide concrete proof at two points.
- Fast pace with no dead air; length matched to a single, contained task.

# CTA placement
- One late-video cost-warning that doubles as a value-add before the ask.
- Paid-community CTA near the end (framed as support for building these on the backend), then like + outro.
