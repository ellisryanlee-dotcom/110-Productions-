---
id: sc-0040
source_video: Gc03J27xmBc
views: "227K"
length: "36:56"
---
# Hook (0:00–0:30)
Outcome + free-template pattern: promises a working n8n system that generates and
publishes faceless shorts on autopilot ("grow a channel while you sleep") and that
all resources are free. Jumps straight into a live demo before any teaching to
prove it works.

# Beat map
- 0:00 hook + promise + free-resource offer → straight into demo
- live end-to-end run: sheet trigger → image prompts → images → videos → audio →
  merge → render → YouTube upload → sheet update → email, narrated as it executes
- shows finished example shorts while async steps wait
- full node-by-node teardown of the same workflow (the long middle)
  - sheet-as-queue, list handling, prompt agent + system prompt design
  - image API (submit/poll), image-to-video, audio + Drive share
  - merge/split-out, render template, YouTube upload, notifications
- recap + expansion ideas (more clips, narration, auto idea generation, scheduling,
  cost breakdown) → community CTA

# Retention devices
Demo-first payoff before the long teardown; cutaways to finished videos during
wait/poll gaps to fill dead time; running cost callouts; "save your workflow after
every node" hard-won-lesson asides; reactive-prompting war stories (why a guardrail
exists) that add authenticity; long-form completeness signaling a definitive
reference.

# CTA placement
Free-template offer stated at the open and repeated (join free community to grab
the JSON). Paid-community tutorial upsell mentioned mid-video; like request and
final community push at the end.
