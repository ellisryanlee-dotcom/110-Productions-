---
id: sc-0050
source_video: jBanaNBY-sM
views: "197K"
length: "29:37"
---
# Hook (0:00–1:00)
Capability-flex hook: enumerates everything one "media agent army" can do (assistant
tasks + create/edit images, make videos, post to socials, and log everything), then
drops straight into a live demo uploading an image and watching agents act.

# Beat map
- 0:00 Capability rundown + free-template promise + first live action.
- ~2:00 Demo chain: name file → edit image (3 variants) → image-to-video ad.
- ~8:00 Demo: email the asset (contact lookup → share → send).
- ~11:00 Demo: multi-platform scrape → compile to a doc → post to a platform.
- ~15:00 Architecture: input switch, main router agent, success/error logging branch.
- ~18:00 System prompt walkthrough; fallback model note.
- ~20:00 Sub-workflow tools deep dive (binary via IDs; polling for video).
- ~24:00 Autonomy vs rigid JSON-prompt aside (what he tried and dropped).
- ~26:00 Scraper setup; cost breakdown; setup guide; CTA.

# Retention devices
Rapid succession of "it just did that" demos front-loaded before any theory (proof
first). Running reveals of unexpected agent behavior (it made a bonus video). Honest
behind-the-scenes ("I tried JSON prompting and it was worse") builds credibility.
Cost breakdown near the end rewards finishers with practical payoff.

# CTA placement
Free-community/template plug woven in early and repeated as "you can grab this."
Affiliate/discount codes mentioned around tool intros. Paid community + like at the
end.
