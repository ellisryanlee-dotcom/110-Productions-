---
id: sc-0047
source_video: KUvSzvFeZls
views: "208K"
length: "34:26"
---
# Hook (0:00–2:30)
Cold in-character demo: a fully working, personality-driven voice assistant handling
real tasks (calendar, email) with witty banter — the finished result plays first as
the hook, before any explanation. Then a second demo showing more capabilities +
verification via execution logs.

# Beat map
- 0:00 Live persona demo (voice assistant doing tasks) — result-first hook.
- ~3:00 Architecture overview (router agent → specialized sub-agents + tools).
- ~5:00 Tool stack intro (three tools) + free-template plug.
- ~6:00 Front end build in the app builder (iterate UI conversationally).
- ~11:00 Voice platform: persona prompt, custom webhook tool, body param, voice clone,
  widget embed.
- ~20:00 Backend: webhook, main routing agent, memory keying, sub-agent workflows.
- ~28:00 Walk sub-executions to verify each agent/tool run; error behavior demo.
- ~33:00 Wrap + CTA.

# Retention devices
Entertaining persona provides continuous novelty/comedy (keeps a 34-min build
watchable). Result-first demo opens a "how did he build this" loop. Verification
segments (open the logs, prove it worked) pay off skepticism. Deliberate friction
shown (tool didn't fire; error message).

# CTA placement
Early + mid soft CTAs to a free community for the template and system prompts (framed
as replication shortcut). Paid community pitch mid-video. Like + wrap at the end.
