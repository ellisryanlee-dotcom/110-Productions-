---
id: sc-0041
source_video: B6k_vAjndMo
views: "222K"
length: "28:07"
---
# Hook (0:00–0:30)
In-progress proof pattern: opens mid-action — brain-dumps a manual process into a
chatbot, pastes it into Claude Code, and shows it build an n8n workflow in ~3
minutes, then reacts to the result. Frames the whole video as a beginner-friendly
walkthrough and argues learning this tool is "the future."

# Beat map
- 0:00 cold proof build + reaction ("look what it did") + why-watch pitch
- setup: VS Code + Claude Code extension, open a project folder, run modes
- create CLAUDE.md as the project system prompt (agent helps write it)
- whiteboard aside explaining the architecture (you → Claude Code → n8n via MCP +
  skills)
- install the n8n-MCP server and n8n skills by handing the agent the repo URLs;
  supply instance URL + API key; restart and verify
- recap of the project layout so far
- live build: plan-mode prompt for a scheduled research→newsletter workflow →
  clarifying questions → plan → create in instance → run → inspect nodes/prompts →
  check the emailed result
- caveats: model/config drift and an overload that throttled the cloud instance →
  CLAUDE.md offered free → close

# Retention devices
Cold-open result before any setup; repeated "don't be intimidated" reassurance for
beginners; whiteboard interlude to re-anchor the mental model; verify-as-you-go
checks; honest limits section (drift, instance crash) for credibility; recaps
between phases to prevent drop-off in a long tutorial.

# CTA placement
Light early framing (why this matters). Mid-video paid-community mention tied to
courses/wireframing. Free CLAUDE.md offered near the end via the free community;
like request and standard sign-off close it.
