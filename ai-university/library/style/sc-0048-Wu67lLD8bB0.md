---
id: sc-0048
source_video: Wu67lLD8bB0
views: "205K"
length: "11:49"
---
# Hook (0:00–0:30)
Superlative + trend-jack ("Google just dropped the most powerful workspace CLI") tied
to a personal pain the viewer shares ("if your stuff lives in Google, you'll love
this"), then an instant capability rundown to prove scope.

# Beat map
- 0:00 Claim + capability overview (what it can touch across Google apps).
- ~1:30 Concrete before/after: raw-markdown docs vs a properly formatted doc, built
  live via a bash command.
- ~3:00 Benefits rundown (one interface, JSON-first, auto-discovery, low overhead) +
  "what is a CLI" explainer for beginners.
- ~5:00 Install walkthrough: let the agent read docs; then manual OAuth setup path.
- ~8:00 Real uses: email triage/scoring; Slides generation + visual-validation loop.
- ~11:00 "It's early, install it now and practice" + CTA.

# Retention devices
Fast before/after payoff early (ugly markdown → clean doc). Beginner explainer inline
keeps novices from bouncing. Shows a limitation honestly (builds blind) then the
clever fix (screenshots), which doubles as a wow moment. "It'll only get better"
future-pacing.

# CTA placement
Repo link early; soft "get it installed and use it" push. Cross-link to a prior
assistant-build video as the suggested next step. Like + wrap at the end.
